# ============================================================================
# sim.do —— 一键启动仿真、加波形、运行到结束
# ============================================================================

do build.do                         ;# 1. 先编译

vsim -novopt work.top_tb            ;# 2. 载入 TB（不开优化便于调试）
log -r /*                           ;# 3. 记录所有信号
add wave -r /*                      ;#    同时加到波形窗口

run 4 us                            ;# 4. 跑到 testbench 自然停                      ;# 5. 批处理场景自动退出
